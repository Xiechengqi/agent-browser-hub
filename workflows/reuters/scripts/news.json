{
  "pipeline": {
    "site": "reuters",
    "name": "news",
    "strategy": "PUBLIC",
    "browser": false,
    "pipeline": [
      {
        "navigate": "https://www.reuters.com/"
      },
      {
        "wait": 2000
      },
      {
        "evaluate": "(() => {\n  const items = [];\n  document.querySelectorAll('[data-testid=\"Heading\"]').forEach(el => {\n    const link = el.closest('a');\n    if (link) {\n      items.push({\n        title: el.textContent.trim(),\n        url: link.href.startsWith('http') ? link.href : 'https://www.reuters.com' + link.href\n      });\n    }\n  });\n  return items;\n})()\n"
      }
    ]
  }
}
