name = "like"
description = "Like a specific tweet"
strategy = "COOKIE"

[[params]]
name = "url"
type = "string"
required = true
positional = true
description = "Tweet URL"

[execution]
entry = "script"
script = "scripts/like.json"

[output]
columns = ["status", "message"]
