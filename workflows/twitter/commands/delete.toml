name = "delete"
description = "Delete a specific tweet by URL"
strategy = "COOKIE"

[[params]]
name = "url"
type = "string"
required = true
positional = true
description = "Tweet URL"

[execution]
entry = "script"
script = "scripts/delete.json"

[output]
columns = ["status", "message"]
