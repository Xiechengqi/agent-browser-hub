name = "friends"
description = "Get Facebook friend suggestions"
strategy = "PUBLIC"

[[params]]
name = "limit"
type = "int"
required = false
positional = false
description = "Number of friend suggestions"
default = 10

[execution]
entry = "script"
script = "scripts/friends.json"

[output]
columns = ["index", "name", "mutual"]
